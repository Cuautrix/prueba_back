CREATE TABLE cat_configuracion
(
    id_configuracion BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    clave            VARCHAR(255),
    valor            VARCHAR(255),
    CONSTRAINT pk_cat_configuracion PRIMARY KEY (id_configuracion)
);